App
-----------
[ ] - Make the main menu look like on the images in discord, have fake data be used to have the table ui be tested and made look nice
      [ ] - Table headers 
            [ ] - Make header resizing be from initial point like for scroll bars
            [ ] - Make resizers be floating and a bit bigger so they are easy to catch and then might as well make the visible space between header smaller
            [ ] - Add dragging of the headers around to be able to reposition
      [ ] - Fix the scroll bar ui
[ ] - Work on debug data passing to pcl to be able to have frame rate ui and such things
      [ ] - Number of batches last frame
      [ ] - Number of pushed of scissor rect last frame
      [ ] - Number of successfull scissor rect changes last frame
[ ] - 
[ ] - Make sure you dont have gsync enabled
[ ] - There is bug with the scroll bar, it keeps its scroll when we filter out the table then when you try to use it it is broken.
[ ] - Keep the header above the scroll list to be able to see which column is which
[ ] - /* These are for later */
[ ] - Defere the resize till next frame for the table to not have different data be used in different PCL_Command__new_main_font_size 


Debug
-----------


Performance
-----------


Rendering
---------
[ ] - Have a way for window to redraw when resizing
[ ] - Have the app be rendered when the window gets resized


Tables
------
[ ] - Add a way to sort the UI when we press a header


UI lib 
--


App UI 
--
[ ] - Is there a way to have border + pading be its own separate thing. Maybe have non padded border be a flag since the default case is when the border is padded or some like that, not sure. But i also like to aply padding after the border as well. Think about it later. This is kind of minor, so dont worry about this too much.


Layers / Architecture
---------------------
[ ] - Add things to layers to provide quick messages when they are used before
      initialization or in the wrong order.
      Since a lot of the layers are stateful, this could help catch misuse.


Platform
--------
[ ] - Better OS event list API
[ ] - DPI support
[ ] - .rc for the app with a custom icon


Commands
--------


Testing (coverage)
-----------
[ ] - Add tests for list macros inside core_base, those are used a lot in the codebase and knowing that those work would be great, also having tests would allow for their api change with no issues for the code that is dependant on them at the time of change


Other/Misc
-----------
[ ] - Fix the render layer handle thing, it is annoying in the way it is right now for reals
