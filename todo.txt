Current:

Performance
-----------
[ ] - r_present seem to be taking too long for not a lot of drawings

Rendering
---------
[ ] - Ask d3d discord about the 5ms present time when having a singl rect to draw
[ ] - Have a way for window to redraw when resizing
[ ] - Have the app be rendered when the window gets resized
[ ] - Don't allow the headers or rows to overdraw the box that they are in
[ ] - Fix that dont_overdraw_children only uses scissor rect for the box and not the whole nested tree of the box
[ ] - Test if no overdraw in UI box works when you have them nested
      Think about this, maybe even with Who, or Claude

[ ] - Add a way to have rounded corners be done in the shader for the UI
[ ] - Test right, left, top, bottom for corner_radius
[ ] - Have UI use pixel corners and not 0-1% corners


Tables
------
[ ] - Add scroll by wheel and with shift to the right and left
[ ] - Have the whole line be a different color when you drag a header
[ ] - Add a way to sort the UI when we press a header

[ ] - The resizers are not offset by the offset in the table. This allows resizing
      when headers are not visible.
      This is a bug. Either don't spawn the resizers, or have them be clip-offset as well.

[ ] - Fix the bug with IDs when there are a lot of headers in the table
[ ] - Fix the bug with the scroll bar where holding the mouse at 0.0 delta per frame
      still moves the offset a bit
[ ] - Add a minimum size in pixels to the table in the main menu


UI
--
[ ] - Locate when you have duplicate id in a build and do some about it
[x] - Have a box have a pointer to itself in the prev build
[x] - Have a way to get rect from box 
[x] - Have a way to get inner rect from box
[x] - Have a way to get scroll offset from box
[ ] - Have a way to get inner content dim sum from box
[ ] - Have scroll be passed between frames in ui boxed
[ ] - Make the api for scroll way better
[ ] - Have a way to get data fro scroll containers with clay and all that
[ ] - Make the API for ellipsized labels better.
      Right now you have to supply the size from the outside in a not very
      intuitive way.
[ ] - Add a scroll bar to the ui_widgets thing to just have a default way to have scroll bars quickly


Layers / Architecture
---------------------
[ ] - Add proper release code for the layers that use it, some of them need that
[ ] - Set up DLL reloading for UI stuff once the app has more data and a proper
      shared header
[ ] - Add things to layers to provide quick messages when they are used before
      initialization or in the wrong order.
      Since a lot of the layers are stateful, this could help catch misuse.
[ ] - Rename DllPop DllRemove  


Platform
--------
[ ] - Better OS event list API
[ ] - DPI support
[ ] - .rc for the app with a custom icon


Commands
--------
[ ] - Close the app