Current:
[ ] - Need to add a profiler to the app

[ ] - Have the whole line be a different color when you drag a header
[ ] - Add a way to have sort for the ui when we press a header

[ ] - The resizers are not offseted by the offset in the table, this allows for resizing when headers are not visible, 
      this is a bug, either dont spawn the resizers, or just have the be clip offsetted as well

[ ] - Fix the bug with ids when there are a lot of headers in the table
[ ] - Fix the bug with the scroll bar that when you hold mouse at 0.0 delta per frame it sill moves a bit, not the mouse, but the offset that the scroll bar produces

[ ] - Have a way for window to redraw when resizing

[ ] - There is a larga posibility behind sudle bug with padding and norders between rows and headers
      It would be great to have those not happend as easily, via some api or some like that.

[ ] - set up reloading dll for ui stuff for the app when there is more data in the app that you have a legit header file for it

[ ] - dont allow the headers or rows to overdraw the box that they are in
[ ] - Fix that dont_overdraw_children only uses scissor rect for the box and not the whole nested tree of the box
[ ] - Test if no overdraw in ui box works when you have them nested
      Think abou this, maybe even with who, or claude

[ ] - Add a way to have rounding corners be done in the shader for the ui
[ ] - Test right,left,top,bottom for corner_radius
[ ] - Have ui use px corners and not 0-1 % corners 
[ ] - Add an external profiler to the app
[ ] - Add things to layers to just have quick messages when we use the layers without having them be initialised or some like that.
      Since a lot of the layers are statefull and have an idea about in which order things are to be used it might help in some cases. 
[ ] - Check if there is a bug with how labels are sized relativelly to how they are draw, seems like they take more space than needed when scaling is used on them to scale the text down
[ ] - Commands to implement
  [ ] - Close the app

[ ] - add min size in px to the table in the main menu

[ ] - Take a look at thow texture and therefore text is drawn and batched, fps is probably low cause of that

[ ] - Make api for elipssed labels better, right now you have to suply the size from the outside in a not really intuitive way

[ ] - Better os event list api
[ ] - Have the app be rendred when the window gets resized
[ ] - DPI support
[ ] - Call Dll pop DllRemove
[ ] - .rs for the app with a custom icon


 
